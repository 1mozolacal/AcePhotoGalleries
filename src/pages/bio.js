import * as React from "react";
import Sidebar from "../components/Sidebar";

// markup
const BioPage = () => {
  return (
    <main>
      <Sidebar activeTab={1}/>
    </main>
  );
};

export default BioPage;
